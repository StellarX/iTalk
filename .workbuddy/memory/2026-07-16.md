# 2026-07-16

- 英语学习网站(italk) 项目推进：完成记忆算法(memoryAlgorithm.js, 9/9 单测通过)、MySQL 5.7 schema(db/schema.sql)、Express 后端全部路由、前端 premium UI、种子数据。
- 用户确认：本地已装 MySQL 5.7，由用户自行建库/建表/启动服务；我方只交付代码与建表 SQL，不在最终交付时启动项目。已清理临时探测脚本 probe.js。
- 向用户提供建库+建表 SQL 及完整启动步骤（.env 配置 → npm install → npm run db:seed → npm start）。

## 真实词库数据（关键交付）
- 用户要求：单词必须是真实的、几千个级别（之前只有占位 20 词/库）。
- 数据源：gaollard/english-vocabulary 仓库。
  - 官方完整 txt 词表（六级/考研/托福，去重后唯一词 3991/5047/10367）作底表。
  - json_original/json-sentence 富数据（uk/us 英美音标 + translations 释义 + phrases 短语 + sentences 例句英中对照）按单词匹配补充。
  - 雅思无官方 txt，直接用 json-sentence 真实雅思词表（5275 词，含音标例句）。
- 重新实现 scripts/build-word-data.js：读取 .cache 原始文件 → 合成四库 → 写出 db/seed-data.js。
  - 结果：总计 24,680 真实单词；97.7% 含音标、93.9% 含例句、0 空释义。
  - 四库：英语六级=3991、考研英语=5047、托福=10367、雅思=5275。
  - 说明：源列表同一 headword 会重复出现（如 specialist 出现 3 次带不同释义），按单词去重后为真·唯一词汇量；DB 唯一键也会拒绝重复。
- 优化灌库：seed.js / setup.js 改为批量 INSERT（每批 500 行），3 万+ 单词秒级完成；package.json 新增 `db:build` 脚本（重跑构建）。
- 沙箱网络限制：raw.githubusercontent 单文件 >~18MB 会被截断、Range 分块 >~8MB 会 ECONNRESET；gaollard 的 json-sentence 单文件均 <8MB（最大 TOEFL_2=6.7MB）可正常下载。zhenghaoyang24 的 24MB 大文件在沙箱不可达（用户本机可跑）。
- 前端 app.js 已对空 phonetic/example/phrase 优雅降级（空音标仍显示 🔊 TTS 按钮用 word 发音，例句/短语缺失显示 —）。
