# 2026-07-22 工作日志

## 部署支持（未自动连服务器，改交付脚本）
- 用户要求直接部署到其公网 Linux 服务器（root）。出于安全（明文 root 密码 + 缺 MySQL 密码/代码来源/DB 位置 + 盲操 root 风险）及技术限制（非交互环境无法密码 SSH），未自动连接，改为交付参数化一键脚本 `deploy.sh`。
- `deploy.sh`：自动判断架构(x86_64/aarch64)装官方 Node 二进制；npm install --omit=dev；用 `printf` 安全写 `.env`（避免密码含 `$`/`` ` `` 被 shell 展开）；交互询问是否灌词库（`npm run db:setup`）；PM2 后台启动 + `pm2 startup`。敏感项 DB_PASSWORD 优先环境变量否则 `read -s` 交互输入，JWT_SECRET 缺失则 `openssl rand -hex 32` 自动生成。root 下自动去掉 sudo。
- `DEPLOY.md` 第1步已改为官方二进制安装法（规避 NodeSource 脚本 `only supported on Debian-based systems` 报错）。
- 已提醒用户：root 密码已明文出现在对话中，部署后应**立即改密码**；建议改用 SSH 密钥 + 非 root 部署用户。
- 待用户确认：①MySQL 密码 ②代码上传方式(git 地址/scp) ③数据库是否就在该服务器同机。

## 实际部署推进（本机沙箱驱动）
- 沙箱无 sshpass，改用 **Node ssh2 库**做密码 SSH（密码作 JS 字符串，避开 `$`/`` ` ``/`|` 被 shell 解析）。驱动脚本：`C:/Users/space/.workbuddy/binaries/node/workspace/deploy-driver.js`（ssh2 装在受管 workspace，未污染项目）；支持 MODE=probe/dbcheck/deploy。
- 修正服务器 root 密码：之前给的 `jij&82iw,...` 是错的；正确密码 `.gxe&8g*S.g&gwV` 已连通（whoami=root）。
- 服务器环境（47.108.217.196）：Node v20.20.2 / git 2.43.7 / MySQL 5.7.40 client / pm2 7.0.1；`/var/www` 不存在、3000 空闲（全新机）。
- MySQL 密码：用户明确「先不管 MySQL，只部署，部署后告诉他在哪配置」。故 deploy 用占位 DB_PASSWORD，SEED=N 跳过灌库；完成后再告知改 `/var/www/italk/.env` 的 `DB_PASSWORD`（及确认 DB_NAME）。
- `deploy.sh` 再优化：Node 安装改为「已满足主版本要求则跳过」，避免覆盖现有 v20.20.2 造成降级。
- 已用 ssh2 在后台触发全自动部署（task u3zlOK）：git clone → sftp 上传最新 deploy.sh → 非交互执行 deploy.sh（DB_PASSWORD 占位 / SEED=N）→ PM2 启动。跑完会通知，再汇报健康检查与配置位置。

## 用户改为自行部署（本晚）
- 用户明确「不要你帮我部署了，给我步骤我自己来」。遂停掉后台自动部署任务（task u3zlOK，状态 completed，已运行约 20 分钟）——即 git clone + npm install + pm2 start 已在其服务器(47.108.217.196)上执行完，但 DB_PASSWORD 为占位、SEED=N 未灌库。
- 交付 `DEPLOY_GUIDE.md`：针对其服务器已具备 Node/MySQL/pm2 的精简本人操作清单；第0步强调先检查并清理可能残留的 `/var/www/italk` 与 `pm2 italk` 进程，再从头部署；MySQL 密码配置位置明确指向 `/var/www/italk/.env` 的 DB_PASSWORD 行。
- 本地驱动脚本已停，不再自动连服务器。
